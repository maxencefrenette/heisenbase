# heisenbase
Fuzzy Chess EGTB

As part of this project, I'm trying to push the limits of how much I can get AI to write code, so
please forgive the AI slop.

## Git hooks
This repo uses pre-commit to run `cargo fmt` and `cargo clippy --fix`. Enable it with:

```bash
prek install
```
